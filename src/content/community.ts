// No real community/Facebook group exists yet -- the CTA renders without a
// link (not clickable) until this is set. Single place to change once there is one.
export const communityCtaHref: string | undefined = undefined;
