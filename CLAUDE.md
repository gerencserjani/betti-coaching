## Agent skills

### Issue tracker

Issues live as GitHub issues in this repo (gerencserjani/betti-coaching), managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout (`CONTEXT.md` + `docs/adr/` at the repo root, created lazily as needed). See `docs/agents/domain.md`.
